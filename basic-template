// This template initializes SGDK, clears the screen, and prints "Hello World!" at coordinates (10, 10) on the screen. You can add your game logic inside the while loop. Note that this is just a basic template, and you'll need to add more code to make a complete game.

#include <genesis.h>

int main()
{
    // Initialize SGDK
    VDP_init();
    
    // Clear the screen
    VDP_drawText("Hello World!", 10, 10);
    
    while(1)
    {
        // Your game loop code goes here
    }
    
    return 0;
}
